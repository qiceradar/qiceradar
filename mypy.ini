[mypy]
strict = True

# I wanted to use this, but the type hinting for findGroup() doesn't seem to
# match the docs that have it returning QgsLayerTreeGroup | None`, so my checks
# for whether it is None are all flagged as unreachable.
warn_unreachable = False

warn_unused_ignores = True

# I'm incrementallly adding types to the files (don't want to deal with all the plot_utils!)
follow_imports = skip

python_version = 3.9

# next up:
# * qiceradar_symbology_widget -- mostly clean, just grumpy about untyped decorator
# * datautils/*
# * plotutils/*

files = download_widget.py, qiceradar_config_widget.py, qiceradar_config.py, qiceradar_controls_window.py, qiceradar_dialogs.py, qiceradar_plugin.py, qiceradar_selection_widget.py, radar_viewer_widgets.py, radar_viewer_window.py
# For now, I'm whitelisting file to check,
# files = *.py

